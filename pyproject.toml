[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "volcal"
version = "1.0.0"
description = "Volatility models: pricing and calibration framework"
readme = "README.md"
requires-python = ">=3.9"

authors = [
  { name = "qjant", email = "REDACTED_EMAIL" }
]

dependencies = [
  "numpy>=1.23",
  "scipy>=1.10",
  "pandas>=1.5",
  "openpyxl>=3.0",
  "numba>=0.57",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "black",
  "ruff",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
